// lib/supabaseClient.js
// Temporary stub so builds don't fail if something imports this.
// Replace later with real Supabase client if needed.

export const supabase = null; // named export
export default supabase;      // default export

